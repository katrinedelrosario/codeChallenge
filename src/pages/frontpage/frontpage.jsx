import Loader from "../../components/loader"
import Tooltip from "../../components/tooltip"

export const Frontpage = () => {

    return (
        <div>
            <h1>Frontpage</h1>
            <Loader />
        </div>
    )
}